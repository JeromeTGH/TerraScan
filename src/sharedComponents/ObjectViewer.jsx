import React from 'react';
import styles from './ObjectViewer.module.scss';

const ObjectViewer = () => {
    return (
        <div>
            
        </div>
    );
};

export default ObjectViewer;